{
  "name": "base-excel-api",
  "version": "1.0.0",
  "description": "to run app in development make sure to install tailwind and env from requiremts.txt",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss build -i ./static/src/input.css -o ./static/dist/output.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:production": "python main.py"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.17",
    "daisyui": "^5.0.9",
    "tailwindcss": "^4.0.17"
  }
}
